import 'package:flutter/material.dart';

class Zen extends StatelessWidget {
  const Zen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('zen'),
    );
  }
}
